terraform {
  backend "azurerm" {
    resource_group_name  = "REPLACE_RG"
    storage_account_name = "REPLACE_STORAGE_ACCOUNT"
    container_name       = "tfstate"
    key                  = "aks-enterprise-platform.tfstate"
  }
}
